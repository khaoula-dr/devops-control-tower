# Politique par défaut : tout bloquer (whitelist explicite ensuite)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: voting
spec:
  podSelector: {}        # S'applique à tous les pods du namespace
  policyTypes:
    - Ingress
    - Egress