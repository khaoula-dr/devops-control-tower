version: '3.8'

services:
  vote:
    image: salmakk/vote:latest
    ports:
      - "5000:80"

  result:
    image: salmakk/result:latest
    ports:
      - "5001:80"

  worker:
    image: TON_USERNAME/worker:latest

