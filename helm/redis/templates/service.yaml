apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
spec:
  type: {{ .Values.service.type }}
  selector:
    app: redis
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 6379
